// PRIMITIVE TYPE

// String Type
const department: string = "Computer";

// Number Type
const roll: number = 350883;

// Boolean Type
const isAdmin: boolean = true;

// Undefine Type
const x: undefined = undefined;

// Null Type
const y: null = null;

// NON-PRIMITIVE TYPE

// Array Type
const friends: string[] = ["Polash", "Sakib", "Anik", "Akram"];

// Tuple Type
const ageName: [number, string] = [28, "Babu"];

// Object Type
const user_1: {
  name: string;
  roll: number;
  department?: string; // Optional Type
} = {
  name: "Riyadh",
  roll: 350883,
};
